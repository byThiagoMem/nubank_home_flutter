// ignore: avoid_classes_with_only_static_members
import '../../../../shared/themes/app_theme.dart';

// ignore: avoid_classes_with_only_static_members
class ListItems {
  static List<String> titles = [
    'Área Pix',
    'Pagar',
    'Tranferir',
    'Depositar',
    'Pegar\nemprestado',
    'Recarga de\ncelular',
    'Cobrar',
    'Doação',
    'Encontrar\natalhos',
  ];

  static List<String> images = [
    AppTheme.images.pix,
    AppTheme.images.bar_code,
    AppTheme.images.transfer,
    AppTheme.images.deposit,
    AppTheme.images.loan,
    AppTheme.images.recharge,
    AppTheme.images.money,
    AppTheme.images.favorite,
    AppTheme.images.question_black,
  ];
}
